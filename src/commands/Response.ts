import { TransmissionPacket } from "../TransmissionPacket";
import { Chars, CommandCode, DisplayPosition, FileLabel, ModeCode, TypeCode } from "../types";

export class Response  {
}

export class GenericResponse extends Response {
    
}
